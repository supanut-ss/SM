// หาช่องว่างพนักงาน × ห้อง × กะ — pure function เท่านั้น ห้าม import Prisma/Nest/React/fetch
// เวลาปัจจุบันรับเป็น parameter เสมอ (ห้ามเรียก Date.now() ตรง ๆ)
// จะเริ่มเขียนจริงใน Task T4.1 (ดู docs/PLAN.md §5, §7.2)
export {};
