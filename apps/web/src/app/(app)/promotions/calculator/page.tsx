import { PromotionCalculatorClient } from "./promotion-calculator-client";

export const metadata = { title: "ทดลองคำนวณโปรโมชั่น — Sabaizy" };

export default function PromotionCalculatorPage() {
  return <PromotionCalculatorClient />;
}
