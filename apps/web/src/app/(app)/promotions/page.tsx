import { PromotionPageClient } from "./promotion-page-client";

export const metadata = { title: "โปรโมชั่น — Lotus Desk" };

export default function PromotionsPage() {
  return <PromotionPageClient />;
}
