import { PackagePageClient } from "./package-page-client";

export const metadata = { title: "คอร์ส/แพ็กเกจ — Lotus Desk" };

export default function PackagesPage() {
  return <PackagePageClient />;
}
