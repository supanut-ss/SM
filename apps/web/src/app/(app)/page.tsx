import { DashboardPageClient } from "./dashboard/dashboard-page-client";

export const metadata = { title: "หน้าหลัก — Sabaizy" };

export default function DashboardPage() {
  return <DashboardPageClient />;
}
