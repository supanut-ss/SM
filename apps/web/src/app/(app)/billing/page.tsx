import { BillingPageClient } from "./billing-page-client";

export const metadata = { title: "บิล/แคชเชียร์ — Lotus Desk" };

export default function BillingPage() {
  return <BillingPageClient />;
}
